[pytest]
testpaths = tests
addopts = -q
# Make the `app` package importable when running `pytest` from this folder,
# so no PYTHONPATH juggling is needed in CI, locally, or on a new machine.
pythonpath = .
